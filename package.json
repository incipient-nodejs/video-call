{
  "name": "express-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "certs": "mkdir -p certs && [ ! -f certs/key.pem ] && openssl req -nodes -new -x509 -keyout certs/key.pem -out certs/cert.pem -days 365 -subj \"/C=US/ST=Dev/L=Local/O=Dev/OU=Dev/CN=localhost\" || true",
    "start": "npm run certs && node server.js",
    "dev": "npm run certs && node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "ngrok": "^5.0.0-beta.2"
  }
}